-----------
HealthCheck
-----------

. features:
  - analysis
    * gastric states graph
    * correlation food => gastric states

  - user administration
    * edit own profile
    * administration
      * view user list (paginate, sort)
      * view user profile
      * edit user profile

  - gastric states
    * use better time picker
    * replace "Save" button by link 

. technical
  - replace JSCalendar by jQuery date picker
  - use HTML 4.01 Strict DTD (or XHTML?)
  - improve Eclipse/Maven integration
  - split up global.css
  - migrate unit tests to JUnit 4 + Spring TestContext

. pain points
  - so many different APIs, so many configuration files (this is Java!)
  - AJAX-enabled pages take longer to develop and are harder to debug (this is Ajax!)
  - bad integration between Eclipse and Maven
  - global.css is getting big (split it up?)
  